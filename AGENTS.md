**项目知识中枢**：所有正式逻辑与决策必须沉淀至 Wiki，代码仅为文档的最终具象化实现。

## 一、 思维原则 (First Principles)

**核心**：运用第一性原理，深挖根因，从物理和逻辑的源头解决问题。

* **根治优先**：出现问题先定位根源，从根因处修复。严禁在表层写 workaround 或打补丁。
* **最小必要**：严格收敛需求边界，只实现当前必须的功能，杜绝画蛇添足的"顺手优化"。
* **质疑假设**：不接受"以前就是这么写的"作为理由，所有技术决策和重构必须有明确的逻辑支撑。

```text
[思维链路推演图]
异常暴露
   │
   ▼
连续追问 WHY (×5) ──→ 锁定核心根因
   │
   ▼
底层逻辑重构/修复 ──→ 验证整条问题链坍塌
   │
   ▼
Task Closed (不附加任何无关操作)
```

## 二、 沟通规范

**核心**：自然语言 + 图表辅助，追求极致的信息信噪比。

* **动态沟通 (对话/确认)**：优先使用简明扼要的 **ASCII 流程图** 或 **Mermaid 语法** 展示逻辑分支，直达核心。
* **静态沉淀 (Wiki/方案)**：必须使用标准的 **Markdown (含完整 Mermaid 架构图)** 进行结构化记录。
* **表达纪律**：禁止大段代码贴图、禁止冗余套话、禁止重复用户已知的上下文。遇到信息缺失必须主动提问，**绝对禁止基于猜测推进**。

## 三、 编码原则

**核心**：大道至简，高内聚低耦合，为未来的维护者写代码。

* **纯粹直接**：采用最直接的实现路径，拒绝过度设计与提前抽象。
* **绝对解耦**：坚持模块化与职责单一。模块间仅通过清晰的接口/参数通信，绝不依赖或修改外部内部实现。
* **代码洁癖**：不保留无效注释、不向未修改的逻辑块添加冗余说明、不引入无用的 Feature Flag、不为历史遗留的脏数据做向后兼容的 Hack。

## 四、 工作流程 (Wiki 技能驱动)

**核心**：Wiki 是唯一信息源，先读 Wiki 再行动。通过 `/wiki:*` 技能驱动四阶段闭环。

```text
/wiki:clarify — 需求澄清
   │
   ├─ Wiki 取证 + 加权评分 (模糊度 ≤ 20%)
   ├─ 边界模糊 → 靶向追问 → 循环
   ▼ 通过
/wiki:plan — 方案设计
   │
   ├─ Wiki 取证 + WHY×5 根因分析
   ├─ 子任务拆分 (目的+预期+流程图+验收)
   ├─ 方案自审: 造轮子? 耦合? 补丁?
   ▼ 用户批准
/wiki:execute — 编码执行
   │
   ├─ 按子任务逐个执行 + 验收循环
   ├─ 复用现有组件，禁止造轮子
   ├─ 代码自审: 造轮子? 耦合? 补丁?
   ▼ 全部验收通过
/wiki:review — 独立审查
   │
   ├─ 四视角: 契约对齐 + 逻辑 + 质量 + 架构健康
   ├─ PASS → 任务完成
   ├─ REVISE/REJECT → 退回 execute
   ▼
Done (Wiki 归档)
```

**Wiki 操作规范**: 所有 Wiki 读写必须通过 Wiki MCP 工具调用，**严禁直接操作 wiki/ 目录下的文件**。

| 操作 | MCP 工具 | 说明 |
| :--- | :--- | :--- |
| 查询文档 | `wiki_doc_search` / `wiki_doc_tree` | 搜索或浏览 Wiki 结构 |
| 读取文档 | `wiki_doc_get` | 读取指定文档内容 |
| 写入文档 | `wiki_doc_write` | 创建或更新文档 |
| 删除文档 | `wiki_doc_delete` | 删除指定文档 |
| 任务管理 | `wiki_task_*` | 创建/查询/更新任务状态 |
| 沟通记录 | `wiki_comm_*` | 添加/查询沟通记录 |

**状态管理**: 每个阶段通过 `.wiki-task` 文件跟踪任务状态，自动记录当前阶段和进度。

**快速通道**: 需求清晰时可跳过 clarify，直接从 `/wiki:plan` 开始。

| 阶段 | 核心动作 | Wiki 联动 | 产出 |
| :--- | :--- | :--- | :--- |
| **澄清** | 加权评分收敛需求边界 | 读取 Wiki 上下文 → 写入澄清 Spec | 模糊度 ≤ 20% 的 Spec |
| **设计** | 根因分析 + 子任务拆分 | 读取 Wiki 架构 → 写入方案契约 | 带验收标准的子任务清单 |
| **执行** | 子任务驱动编码 + 验证循环 | 读取 Wiki 方案 → 写入执行报告 | 验收通过的代码 |
| **审查** | 四视角盲审 + 架构健康 | 读取 Wiki 全部文档 → 写入审查报告 | PASS / REVISE / REJECT |

## 五、 绝对红线 (Red Lines)

1.  **Wiki 先行**：任何阶段开始前，必须先读取 Wiki 知识库定位上下文。未读 Wiki 就行动 → 违规。
2.  **MCP 调用**：Wiki 读写必须通过 Wiki MCP 工具，严禁直接读写 `wiki/` 目录文件。
3.  **禁绝猜测**：不确定的前置条件，必须挂起任务并向用户求证。
4.  **严禁跳步**：无方案不编码，无授权不执行。
5.  **拒绝妥协**：不写补丁代码，不在表层绕过问题。
6.  **克制执行**：不加用户没要求的东西（No extra features）。
7.  **不造轮子**：有现成组件不复用而新建 → 违规。
